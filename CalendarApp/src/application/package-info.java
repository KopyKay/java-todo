/**
 * Pakiet {@code application} zawiera kluczowe klasy, które tworzą główną
 * strukturę aplikacji kalendarza. W ramach tego pakietu znajdują się klasy
 * zarządzające logiką aplikacji, interakcją z użytkownikiem, obsługą interfejsu
 * użytkownika, a także komunikacją z bazą danych i serializacją danych. Klasy w
 * tym pakiecie są skoncentrowane wokół centralnego kontrolera
 * ({@link application.Controller}), który pełni kluczową rolę w synchronizacji,
 * sortowaniu, aktualizacji i usuwaniu danych, a także dostarcza funkcje
 * dodawania nowych jednostek oraz walidacji integralności danych. Dodatkowo,
 * pakiet zawiera klasę główną programu ({@link application.Program}), która
 * umożliwia uruchomienie aplikacji w trybie konsolowym lub z interfejsem
 * graficznym.
 *
 * @author Mateusz Kopaczewski
 */

package application;